// @ts-check

const Sequelize = require("sequelize");
const sequelizeConnection = require("../DB/database").sequelizeConnection;

const UnderwrittersModel = require("./underwriters")

const EducationPolicy = sequelizeConnection.define("DomesticPolicyUnderwritters", {
    
})